const { opttions } = require("./sell");

module.exports ={
    name:'buy',
    description:'Buy a deck or card from the shop',
  
    callback: (client,interaction) => {
        interaction.reply('This command is still in development');
    }
}